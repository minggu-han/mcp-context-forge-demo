spring:
  data:
    # redis 配置
    redis:
      # 地址
      host: 10.18.1.174
      # 端口，默认为6379
      port: 6379
      # 数据库索引
      database: 3
      # 密码
      password: msikredis1108
      # 连接超时时间
      timeout: 10s
ollama:
  baseUrl: http://localhost:11434
  modelName: qwen3.5:4b